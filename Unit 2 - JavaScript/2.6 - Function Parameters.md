###### ICS3U U2: JavaScript - Mr. Brash 🐿️

# 2.6 - Function Parameters

<img src=".assets/params_and_return.png" width="360px"  style="display:block;margin:auto;filter:drop-shadow(3px 3px 4px grey)">  

<br>

**If you missed the live demonstration in class, read below. Otherwise you can go straight to [your task](#-your-task).**

- Don't like reading? [Here's a video on _Function Parameters_](https://youtu.be/hI-04Ot4vZY)


## Lesson: Function Parameters

**Ever notice that `Math.floor(x)` takes in a number but `Math.random()` doesn't?**  
And `console.log()` can take _many_ values, separated by commas. 

Those values inside the brackets are called _**parameters**_. They are the _inputs_ to a function and allow us to _pass_ values around in our code. Here's an example of a **custom function with parameters**:
```JS
// Raise a base to an exponent and print the result
function exponent(base, exp) {
    let answer = base**exp;
    console.log(answer);    // Could also be done without the variable
}
```

☝🏻 The parameters to that function are `base` and `exp`. When we _call_ the `exponent` function from now on, we have to give values in those locations. The values we give it are called _arguments_ but just think of them as _inputs_ or _parameters_.
```JS
exponent(5, 2);   // prints 25
exponent(10, 3);  // prints 1000

// What will this print:
exponent()

// Or this:
exponent(6)
```

With _parameters_ we can start customizing our functions to do exactly what we want:
```JS
rollDice(4, 6);    // Roll four 6-sided dice for a total value

randInt(-12, 12);  // Get a random whole number from -12 to 12
```

### 💻 Your Task

**Important:** Only #4 of the following functions should use `prompt()`, the rest should take _parameters_.

Go to the [main.js code file](./main.js) and create these new custom functions:

1. `add3(num1, num2, num3)` - adds the values of the variables `num1`, `num2`, and `num3` and _prints_ the result. Go to your dev console and run the function several times with different parameters to test it.

2. At this point we should know how to get a random percentage value and multiply it by a value to get a random number from 1 to x:
    ```JS
    Math.ceil(Math.random() * x)
    ```
    Create the function `roll(sides)` which rolls a single die with `sides` number of sides and prints the result to the console.

3. `rollDice(sides)` - rolls **two** dice with `sides` number of sides and adds the result, printed to the console.

4. `userRoll()` - this function does _not_ have any parameters. Instead, it will ask the _user_ - via `prompt()` - how many sides are on the dice and then call the `rollDice(sides)` function you created in #3. Notice that the _user_ doesn't get to see the result? More on that later...

**Challenges**

5. `pythag(a, b)` - calculates the length of a hypotenuse, rounded to the nearest hundredth (two decimals), given sides `a` and `b`.

6. `round(value, decimals)` - **This one requires a math brain - do _not_ search for a solution, do it yourself!**  
  Rounding to the nearest whole number with `Math.round()` is useful, but what if we want the tenths, hundredths, or thousandths of a value? Create the function `round(value, decimals)` that _returns_ the `value` rounded to the requested number of `decimals`. For example: `round(Math.PI, 4)` would return 3.1416 but `round(0.65, 0)` returns 1.

<br>


🐿️


---

<table style="width: 100%;">
  <tr>
    <td align="left" valign="top"><a href="2.5 - Functions.md">2.5 - Functions</a></td>
    <td align="center"><a href="2.0 - Table of Contents.md">Table of contents</a><br><br>(2.6 - Function Parameters)</td>
    <td align="right" valign="top"><a href="2.7 - Return.md">2.7 - Return</a></td>
  </tr>
</table>